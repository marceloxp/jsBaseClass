{
    "filewatcher.commands": [
        {
            "match": "\\.*/src/.*.js",
            "isAsync": true,
            "cmd": "cd ${workspaceRoot} && gulp",
            "event": "onFileChange"
        }
    ]
}