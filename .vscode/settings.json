{
    "filewatcher.commands": [
        {
            "match": "\\.*/src/.*.js",
            "isAsync": true,
            "cmd": "cd ${workspaceRoot} && npm run build",
            "event": "onFileChange"
        }
    ]
}